/**
 * All general type of constants which are not used in other constant files
 * example:
 * CONSTANT.MOUSE_CLICK = 'onclick'
 */

const CONSTANT = {
    MOUSE_ENTER: "mouseenter",
    MOUSE_LEAVE: "mouseleave",
    DEFAULT_FORM_FIELD_SIZE: {
        xs: 12,
        sm: 12,
        md: 12,
        lg: 6,
        xl: 6,
        xxl: 4,
    },
};
